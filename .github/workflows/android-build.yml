name: build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ORG_GRADLE_PROJECT_disableInstrumentationAgent: "true"
      # (дополнительно) GRADLE_OPTS можно добавить, если нужно

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Clear Gradle cache on runner
        run: rm -rf ~/.gradle/caches || true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Android SDK
        uses: android-actions/setup-android@v2

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build APK (force clean)
        run: ./gradlew clean assembleDebug --no-daemon --no-configuration-cache

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/*.apk
